# secure-pr-reviewer
A security-first AI pull-request reviewer that uses a bounded agent to inspect code changes while defending against prompt injection, excessive tool permissions, and unsafe model output.
